# my omarchy dotfiles

# installation

download this folder to ~

install gnu stow 

arch:
`sudo pacman -S stow`

macos:
`brew install stow`

use stow to apply:

`cd ~/.dots/`
`stow .`

to remove:
`stow -D .`
