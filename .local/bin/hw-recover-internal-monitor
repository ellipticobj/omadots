#!/bin/bash

# :summary=Clear the internal-monitor-disable toggle if no external display is connected.

TOGGLE="$HOME/.local/state//toggles/hypr/internal-monitor-disable.conf"

if [[ -f $TOGGLE ]] && ! hw-external-monitors; then
  rm -f "$TOGGLE"
fi
