#!/bin/bash

# :summary=Overwrite the user configs for swayosd (controls on-screen feedback for changing volume/songs etc) with the  defaults and restart the service.

refresh-config swayosd/config.toml
refresh-config swayosd/style.css
restart-swayosd
