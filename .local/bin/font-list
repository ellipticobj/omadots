#!/bin/bash

# :summary=List available monospace fonts
# :examples= font list |  font set "CaskaydiaMono Nerd Font"

fc-list :spacing=100 -f "%{family[0]}\n" | grep -v -i -E 'emoji|signwriting|' | sort -u
