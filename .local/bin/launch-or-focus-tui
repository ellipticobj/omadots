#!/bin/bash

# :summary=Launch a TUI or focus an existing terminal window for it
# :args=<command> [args...]

APP_ID="org.yuri.$(basename "$1")"
LAUNCH_COMMAND="launch-tui $@"

exec launch-or-focus "$APP_ID" "$LAUNCH_COMMAND"
