#!/bin/bash

# :summary=Toggle hypridle idle locking

if pgrep -x hypridle >/dev/null; then
  pkill -x hypridle
  notify-send -u low "Stopped locking computer when idle"
else
  uwsm-app -- hypridle >/dev/null 2>&1 &
  notify-send -u low "Locking computer when idle"
fi

pkill -RTMIN+9 waybar
