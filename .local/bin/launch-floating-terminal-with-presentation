#!/bin/bash

# :summary=Launch a floating terminal with the  presentation wrapper
# :args=<command>

cmd="$*"
exec setsid uwsm-app -- xdg-terminal-exec --app-id=org..terminal --title= -e bash -c "show-logo; $cmd; if (( \$? != 130 )); then show-done; fi"
